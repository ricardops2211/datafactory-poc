name: Ejecutar deploy ADF

on:
  push:
    branches:
      - nuevo_datafactry

jobs:
  ADF:
    uses: ./.github/workflows/deploy_etl_datafactory.yml
    with:
      branch: "nuevo_datafactry"
    secrets:
      ACCOUNT_KEY: ${{ secrets.ACCOUNT_KEY }}
